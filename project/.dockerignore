# Build context = project root (see backend/Dockerfile).
# We exclude everything that is not necessary for the image to speed up the build
# and avoid including secrets or local artifacts.
**/target/
**/.git/
**/.idea/
**/.vscode/
**/*.iml
.env
**/.env
**/node_modules/
**/.DS_Store
*.log
